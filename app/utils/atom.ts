import { atom } from "jotai";

export const claimsAtom = atom([]);
export const paymentsAtom = atom([]);
export const submittedClaimAtom = atom(false);
export const submittedPaymentAtom = atom(false);
